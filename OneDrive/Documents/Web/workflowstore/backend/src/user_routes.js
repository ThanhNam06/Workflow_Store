export default function registerUserRoutes(app, db) {
  // placeholder for future user routes; keep minimal so import doesn't fail
  app.get('/api/ping-user-routes', (req, res) => res.json({ ok: true }))
}
